apiVersion: 1

providers:
  - name: 'EthHook Dashboards'
    orgId: 1
    folder: 'EthHook'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/dashboards
